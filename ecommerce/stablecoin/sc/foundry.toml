[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200
via_ir = false

[profile.ci]
fuzz = { runs = 100 }
invariant = { runs = 10 }

[rpc_endpoints]
anvil = "http://localhost:8545"

# [etherscan]
# anvil = { api_key = "your_api_key_here" }


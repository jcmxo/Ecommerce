/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Las variables NEXT_PUBLIC_* se exponen automáticamente,
  // no es necesario definirlas aquí a menos que queramos valores por defecto
}

module.exports = nextConfig

